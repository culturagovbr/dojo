import { MapasDirective } from './mapas.directive';

describe('MapasDirective', () => {
  it('should create an instance', () => {
    const directive = new MapasDirective();
    expect(directive).toBeTruthy();
  });
});
